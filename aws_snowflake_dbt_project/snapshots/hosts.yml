snapshots:
  - name: hosts
    relation: ref('dim_hosts')
    config:
      schema: gold
      database: AIRBNB
      unique_key: HOST_ID
      strategy: timestamp
      updated_at: HOST_CREATED_AT
      dbt_valid_to_current: to_date('1999-12-31')
